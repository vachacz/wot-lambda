// (S | N | B) for string, number, binary

var table_wot_tanks = {
    TableName: 'wot_tanks',
    KeySchema: [{
            AttributeName: 'tank_id',
            KeyType: 'HASH',
    }],
    AttributeDefinitions: [{
            AttributeName: 'tank_id',
            AttributeType: 'N',
    }],
    ProvisionedThroughput: {
        ReadCapacityUnits: 1,
        WriteCapacityUnits: 1,
    },
};

var table_wot_import = {
    TableName: 'wot_tanks',
    KeySchema: [{
            AttributeName: 'import_uuid',
            KeyType: 'HASH',
    }],
    AttributeDefinitions: [{
            AttributeName: 'tank_id',
            AttributeType: 'N',
    }],
    ProvisionedThroughput: {
        ReadCapacityUnits: 1,
        WriteCapacityUnits: 1,
    },
};

var table_wot_player_stats = {
    TableName: 'wot_player_stats',
    KeySchema: [{
            AttributeName: 'key',
            KeyType: 'HASH',
    }, {
            AttributeName: 'import_uuid',
            KeyType: 'RANGE',
    }],
    AttributeDefinitions: [{
            AttributeName: 'key',
            AttributeType: 'S',
    },
    {
            AttributeName: 'import_uuid',
            AttributeType: 'S',
    }],
    ProvisionedThroughput: {
        ReadCapacityUnits: 1,
        WriteCapacityUnits: 1,
    },
};

dynamodb.createTable(table_wot_tanks, function(err, data) {
    if (err) ppJson(err); // an error occurred
    else ppJson(data); // successful response
});
